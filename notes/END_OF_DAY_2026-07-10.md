# End-of-Day Summary — cpp-vgoswec CC/DOE
**Date:** 2026-07-10 · **User:** salhus

---

## ✅ Done Today
- CC validated against frequency-domain Budal–Falnes bound **in the time domain** (η 0.84–0.99 at resonance across flaps).
- Grid refined to 0.25 s. Dual-masking (linear-invalid + B55-notch) working.
- **Full CC sweep complete on LOCAL binary:** VGM-0, 10, 20, 45, 90 ✅
- ff+PID sweep: **running overnight — confirm in AM.**

## 📊 Complete CC DOE — trend across flap angle
| VGM | Peak η | Peak-η T | Reactive ratio @ peak | Notes |
|-----|--------|----------|-----------------------|-------|
| 0   | 0.94   | 1.50     | ~0.68                 | η>1 band at T≤1.25 |
| 10  | 0.90   | 1.25     | ~0.71                 | η>1 band at T≤1.0 |
| 20  | 0.84   | 1.25     | ~0.74                 | η>1 band at T≤1.0 |
| 45  | 0.99   | 0.75     | **~0.18**             | low reactive — co-design money point |
| 90  | 0.91   | 1.00     | ~0.70                 | resonance sharpened, shifted short |

Monotonic, physical: flap angle ↑ → resonance sharpens & shifts to shorter T.

## ⚠️ Critical Data Note
Fresh local-binary data **differs materially** from committed CSVs (VGM-0 peak moved 0.75→1.5, values up to 13× larger). **Fresh data is correct/more-physical — commit it, discard committed.**
Optional gap check: `git diff 7072734 9d98a49 -- config/vgoswec_0_cc.yaml`

---

## 🎯 Tomorrow — In Order

**1. Confirm sweep finished (FIRST)**
```bash
ls -la analysis/cc/*.csv analysis/passive_guarded/*.csv
```
All 5 CC + 5 ff+PID present, no `RuntimeError`.

**2. η-handling fix** (~15 lines: `compute_and_write_csvs` + 2 plot fns) — AGREED RULE:
- `0 ≤ η ≤ 1.0` → report normally
- `1.0 < η ≤ 1.10` → **report + flag `linear_popt_invalid` + plot distinctly** (show the ~105% short-period physics)
- `η > 1.10` → **mask** (interp garbage)
- `η < 0` → **mask** (negative-capture tail noise — seen VGM-45 & VGM-90, e.g. VGM-90 T=5.0 η=−5.5e-4)
- Flagged points: distinct style + legend "η > 1: linear P_opt invalid (short-period)" + caption.

**3. Regenerate figures from fresh data → commit real-sim set**

**4. Co-design figure (the payoff)**
- Reactive-ratio vs. capture, one curve per VGM config, CC vs ff+PID overlay → flap config shifts capture/reactive Pareto.
- Claim: optimal flap config is **controller-dependent** (reactive-capable CC vs reactive-limited ff+PID). VGM-45 = demonstrator.
- ⚠️ Compute reactive ratio **across operating band**, not single peak rows.

## 🔵 Deferred (non-blocking)
- `.gitignore` figures (`analysis/**/figures/*.png`) — kills recurring pull conflicts.
- Verify B55/F_exc **normalization** in h5 extraction → "provably recreated Budal."
- Benign negative-P_capture/B55 tail points (T>5) — masked noise, note in writeup, no fix.

## 🏆 The Story
CC recovers 84–99% of theoretical bound at resonance (time-domain reconstruction of a frequency-domain limit); flap config is a **co-design knob** shifting the capture/reactive Pareto **differently per controller** — a real plant-controller co-design contribution.
